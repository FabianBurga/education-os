# M6 v0.7.2 Patch

Endurece el límite staff/family aplicando `require_staff_access` directamente a los
routers M1–M5, en vez de depender de cómo el router agregador los compone.

Sin cambios de base de datos ni contratos REST.
