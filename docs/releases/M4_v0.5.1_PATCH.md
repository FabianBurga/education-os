# M4 v0.5.1 Patch

Corrige el test de integración de tenant vacío del Rector Intelligence Dashboard.
El test ahora aplica directamente el contexto RLS PostgreSQL y no depende de la firma
interna de `apply_tenant_context()`.

No hay cambios de esquema ni API.
