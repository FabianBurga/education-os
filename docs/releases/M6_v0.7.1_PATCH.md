# M6 v0.7.1 Patch

Corrige la prueba del staff/family boundary.

La ruta pública `/api/v1/students` proviene del router API v1, donde internamente se
registra como `/students`. La prueba ahora inspecciona ese router directamente y
confirma que `require_staff_access` está aplicado.

Sin cambios de esquema ni API.
