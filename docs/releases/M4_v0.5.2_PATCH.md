# M4 v0.5.2 Patch

Corrige el helper escalar del Rector Intelligence Dashboard.

`SQLModel Session.exec(text(...)).first()` devuelve una fila SQLAlchemy. M4 ahora
desempaqueta explícitamente `row[0]` antes de convertir contadores y métricas escalares.

No hay cambios de esquema, migración ni API.
